export type HSLColor = {
  h: number,
  s: number,
  l: number;
};

export type HSVColor = {
  h: number,
  s: number,
  v: number;
};

export type RGBColor = {
  r: number,
  g: number,
  b: number;
};